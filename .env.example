# Pharmacy sites are defined in tests/fixtures/pharmacies.ts
# Add or remove entries in that file — no .env changes needed.

# Optional: set BASE_URL to override all pharmacies and run against a single site.
# Useful in CI or for quick one-off runs.
# BASE_URL=https://thepharmacist.healthya.co.uk
